import { ScreenContainer } from '@/src/components/screen-container';

export default function DrawerSettingsScreen() {
  return (
    <ScreenContainer
      title="Settings"
      description="Drawer-level settings screen for quick account and app changes."
    />
  );
}

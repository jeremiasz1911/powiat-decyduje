# Used by Firebase Functions emulator (functions/.env) and deployed runtime env.
SMS_CODE_SECRET=replace-with-a-long-random-secret

# SMSAPI.pl (production: set SMS_API_TOKEN via Firebase Secret Manager)
SMS_PROVIDER=smsapi
SMS_FROM=DecydujApp
# SMS_API_TOKEN=only-for-local-emulator-testing

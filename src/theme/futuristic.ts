import { appShadows, appTheme } from './app-theme';

// Backward-compatible export for legacy imports while the app migrates
// to src/theme/app-theme.ts.
export const futuristicTheme = appTheme;
export const futuristicShadows = appShadows;
